#include <cstdio>
#include <cstring>
const int MAXN = 120;
char left[MAXN];
int ln;
char right[MAXN];
int rn;
char temp[MAXN];

void getinfo(char dst[], int n){
  int len = strlen(temp);
  for(int i = 0; i<len; i++){
  }
}

int main (int argc, char *argv[]) {
  int n;
  scanf("%d", &n);
  return 0;
}
